# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/
node_modules/
# Package Files #
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*

# Build folder
/target/

# Maven Wrapper cache
.mvn/wrapper/maven-wrapper.jar

# STS (Spring Tool Suite)
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

# IntelliJ IDEA
.idea
*.iws
*.iml
*.ipr

# NetBeans
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/

# VS Code
.vscode/

# Local Environment Configuration (VERY IMPORTANT)
application-local.properties```

---

### 3. 📁 `application.properties` (Cập nhật hoàn chỉnh)

File này chỉ chứa các cấu hình chung và các biến giữ chỗ (placeholders).

**Thay thế toàn bộ nội dung file `src/main/resources/application.properties` của bạn bằng đoạn mã này:**

```properties
# Kích hoạt profile "local" theo mặc định.
# Trên server production (Render), chúng ta sẽ đặt biến môi trường SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=local

# ===============================================
# CẤU HÌNH CHUNG (AN TOÀN ĐỂ COMMIT)
# ===============================================

# JPA
spring.jpa.show-sql=true

# Backend / Frontend URL
app.backend.url=http://localhost:8080
app.frontend.url=http://localhost:3000

# ===============================================
# CẤU HÌNH NHẠY CẢM (SẼ ĐƯỢC ĐỊNH NGHĨA Ở CÁC FILE PROFILE)
# ===============================================

# Database credentials
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER}

# JPA dialect and DDL
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO}
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM}

# JWT secret
JWT_SECRET=${JWT_SECRET_KEY}
JWT_EXPIRATION=${JWT_EXPIRATION_MS}