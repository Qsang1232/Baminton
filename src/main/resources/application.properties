# =======================================================
# 1. CẤU HÌNH DATABASE (Lấy từ ảnh Aiven của bạn)
# =======================================================
# Lưu ý: Mình đã thêm tiền tố 'jdbc:' và các tham số chống rớt mạng vào URL
spring.datasource.url=jdbc:mysql://badminton-db-nqs201105-4df9.j.aivencloud.com:12028/defaultdb?ssl-mode=REQUIRED&useSSL=true&requireSSL=true&verifyServerCertificate=false&allowPublicKeyRetrieval=true&autoReconnect=true&connectTimeout=30000

# Username (Lấy từ ảnh)
spring.datasource.username=avnadmin

# Password (QUAN TRỌNG: Bạn hãy tự điền mật khẩu vào dòng dưới)
# Bấm vào nút "Copy" ở dòng Password trên web Aiven rồi dán vào đây
spring.datasource.password=AVNS_jVYi6mp3uGeF7HaNLC-

# =======================================================
# 2. CẤU HÌNH HIBERNATE (Tự động tạo bảng)
# =======================================================
# Dùng 'update' để giữ dữ liệu cũ. Nếu muốn xóa sạch làm mới thì đổi thành 'create'
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# =======================================================
# 3. CẤU HÌNH SERVER CHO RENDER
# =======================================================
# Render sẽ tự điền cổng vào biến PORT. Nếu chạy local thì dùng 8080
server.port=${PORT:8080}

# =======================================================
# 4. TỐI ƯU HIỆU SUẤT & JWT
# =======================================================
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Giữ nguyên cấu hình JWT cũ của bạn
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000
application.security.jwt.refresh-token.expiration=604800000